<%= form_for current_user.inscriptions.find_by_tournament_id(@tournament),
             :html => { :method => :delete } do |f| %>
  <div class="actions"><%= f.submit "Desinscription" %></div>
<% end %>
